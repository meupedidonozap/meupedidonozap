

# Ordenar Produtos em Ordem Alfabética

## Mudança

### Arquivo: `src/hooks/useProducts.ts`

Na query de `useProducts`, trocar `.order('created_at', { ascending: false })` por `.order('name', { ascending: true })` para que os produtos venham em ordem alfabética (A-Z).

